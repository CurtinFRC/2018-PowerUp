#include "curtinfrc/math.h"
#include "DriveBase.h"
#include "IO.h"

using namespace curtinfrc; // Curtin's math functions

// Tank control object

void DriveControl::set_left(double speed) {
  for(,,) {
    left_motors[0]->Set(output);
  }
}
void DriveControl::set_right(double speed){

}
